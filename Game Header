import { Button } from "@/components/ui/button";
import { Package, Save, LogOut, Menu } from 'lucide-react';
import {
  DropdownMenu,
  DropdownMenuContent,
  DropdownMenuItem,
  DropdownMenuTrigger,
  DropdownMenuSeparator,
} from "@/components/ui/dropdown-menu";

export default function GameHeader({ onOpenInventory, onSave, onNewGame, isSaving }) {
  return (
    <header className="flex items-center justify-between py-4 px-1">
      <div className="flex items-center gap-3">
        <div className="text-3xl">⚔️</div>
        <div>
          <h1 className="text-xl font-bold text-amber-100">Realm of Shadows</h1>
          <p className="text-xs text-slate-500">AI Text Adventure</p>
        </div>
      </div>

      <div className="flex items-center gap-2">
        <Button
          variant="ghost"
          size="icon"
          onClick={onOpenInventory}
          className="text-slate-400 hover:text-amber-200 hover:bg-slate-800/50 rounded-xl"
        >
          <Package className="w-5 h-5" />
        </Button>

        <DropdownMenu>
          <DropdownMenuTrigger asChild>
            <Button
              variant="ghost"
              size="icon"
              className="text-slate-400 hover:text-amber-200 hover:bg-slate-800/50 rounded-xl"
            >
              <Menu className="w-5 h-5" />
            </Button>
          </DropdownMenuTrigger>
          <DropdownMenuContent align="end" className="bg-slate-900 border-slate-700 text-slate-200">
            <DropdownMenuItem 
              onClick={onSave}
              disabled={isSaving}
              className="hover:bg-slate-800 focus:bg-slate-800 cursor-pointer"
            >
              <Save className="w-4 h-4 mr-2" />
              {isSaving ? 'Saving...' : 'Save Game'}
            </DropdownMenuItem>
            <DropdownMenuSeparator className="bg-slate-700" />
            <DropdownMenuItem 
              onClick={onNewGame}
              className="text-red-400 hover:bg-slate-800 focus:bg-slate-800 cursor-pointer"
            >
              <LogOut className="w-4 h-4 mr-2" />
              New Game
            </DropdownMenuItem>
          </DropdownMenuContent>
        </DropdownMenu>
      </div>
    </header>
  );
}